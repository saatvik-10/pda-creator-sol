[package]
name = "pda-creator"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "4.0.0"
solana-system-interface = { version = "3.2.0", features = ["bincode"] }
